# React and React Native are peerDependencies provided by the consuming app.
# This package does not install React Native locally (tests run against a
# lightweight stub), so don't auto-install or hard-validate peer deps here.
legacy-peer-deps=true
